{
  "name": "@codeblock/languages",
  "version": "1.0.0",
  "main": "",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf lib && rm -rf .tsbuildinfo",
    "rebuild": "yarn clean && yarn build",
    "prepublishOnly": "yarn rebuild"
  },
  "dependencies": {
    "@codeblock/core": "^1.0.0"
  }
}
