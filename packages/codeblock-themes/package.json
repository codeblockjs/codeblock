{
  "name": "@codeblock/themes",
  "description": "available prism themes and functionality for @codeblock packages",
  "version": "2.0.4",
  "main": "",
  "license": "MIT",
  "author": "Jovica Aleksic <jovica.aleksic@loopmode.de>",
  "repository": {
    "type": "git",
    "url": "git@github.com:codeblockjs/codeblock.git",
    "directory": "packages/codeblock-themes"
  },
  "homepage": "https://github.com/codeblockjs/codeblock/tree/master/packages/codeblock-themes",
  "keywords": [
    "react",
    "prism",
    "highlighting"
  ],
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prebuild": "node prebuild",
    "build": "tsc",
    "watch": "node prebuild && tsc --watch",
    "clean": "rm -rf lib && rm -rf .tsbuildinfo",
    "rebuild": "yarn clean && yarn build",
    "prepublishOnly": "yarn rebuild"
  },
  "dependencies": {
    "@codeblock/core": "^2.0.3",
    "jotform-css.js": "^1.0.1"
  }
}
